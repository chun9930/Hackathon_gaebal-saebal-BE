spring:
  datasource:
    url: jdbc:h2:mem:mcm_private_circle_test;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        jdbc:
          time_zone: Asia/Seoul
  h2:
    console:
      enabled: false

app:
  mock-data:
    seed-enabled: false
  jwt:
